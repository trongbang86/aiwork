import type { FastifyInstance } from 'fastify';
import type { Sqlite } from './db/index.js';
import type { AiContextService } from './services/ai-context.js';

const esc=(value:unknown)=>String(value??'').replace(/[&<>"']/g,(char)=>({ '&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;' })[char]!);
const css=`:root{font-family:Inter,ui-sans-serif,system-ui;color:#17202d;background:#f3f5f8}*{box-sizing:border-box}body{margin:0}a{color:inherit;text-decoration:none}.shell{display:grid;grid-template-columns:230px minmax(0,1fr);min-height:100vh}.rail{background:#101721;color:#dce5ef;padding:22px;position:sticky;top:0;height:100vh}.brand{font-weight:850;font-size:20px}.brand i{color:#82f0c2;font-style:normal}.rail p{color:#8190a2;font-size:12px}.rail nav{margin-top:20px}.rail nav a{display:block;padding:10px 12px;margin:4px 0;border-radius:9px;color:#aebaca}.rail nav a:hover,.rail nav a.active{background:#1d2a38;color:white}.site-search{display:flex;margin-top:22px}.site-search input{width:100%;min-width:0;padding:9px;border:1px solid #344252;border-radius:8px 0 0 8px;background:#182332;color:white}.site-search button{border-radius:0 8px 8px 0}.main{padding:32px clamp(20px,3vw,42px);max-width:1500px;width:100%;margin:auto}.top{display:flex;justify-content:space-between;align-items:flex-start;gap:20px}.eyebrow{font-size:11px;text-transform:uppercase;letter-spacing:.15em;color:#6b7788;font-weight:750}h1{margin:6px 0 28px;font-size:clamp(25px,3vw,32px);line-height:1.15}h2{font-size:17px;margin:26px 0 10px}.pill{padding:6px 10px;border-radius:99px;background:#e3f7ee;color:#16714d;font-size:12px;white-space:nowrap}.grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:16px}.card{background:white;border:1px solid #dfe4eb;border-radius:14px;padding:20px;box-shadow:0 2px 8px #1c273408}.card:hover{border-color:#aab8ca}.stat{font-size:30px;font-weight:800}.muted{color:#6b7788}.board{display:grid;grid-template-columns:repeat(4,minmax(230px,1fr));gap:16px;overflow:auto;padding:2px 2px 16px}.lane{background:#e9edf2;border:1px solid #e0e5eb;border-radius:14px;padding:14px;min-height:520px}.lane h2{display:flex;justify-content:space-between;text-transform:uppercase;font-size:12px;letter-spacing:.07em;margin:2px 2px 12px}.ticket{display:block;background:white;border:1px solid #dce2e9;border-radius:11px;padding:14px;margin:10px 0;box-shadow:0 2px 6px #1c273408}.ticket:hover{border-color:#9facbd;transform:translateY(-1px)}.ticket strong,.ticket span{display:block;margin-top:5px}.ticket small{color:#697688}.actor{display:inline-flex!important;margin-top:12px;background:#eeeaff;color:#5640a5;padding:4px 7px;border-radius:7px;font-size:10px}.tree{background:white;border:1px solid #dfe4eb;border-radius:14px;overflow:hidden}.tree a{display:grid;grid-template-columns:90px 90px 1fr 110px;padding:12px 15px;border-bottom:1px solid #edf0f4}.tree a:hover{background:#f7f9fb}.type{text-transform:uppercase;font-size:10px;color:#758196}.breadcrumbs{display:flex;align-items:center;gap:7px;flex-wrap:wrap;margin:0 0 8px;color:#6b7788;font-size:12px}.breadcrumbs a{color:#4f3cb2;font-weight:700;border-radius:5px;padding:4px 6px;margin-left:-6px}.breadcrumbs a:hover,.breadcrumbs a:focus-visible{background:#ece9ff;outline:none}.breadcrumbs .separator{color:#a7afba}.context{display:grid;grid-template-columns:minmax(0,1.55fr) minmax(280px,.65fr);gap:24px;align-items:start}.prompt{white-space:pre-wrap;background:#101721;color:#d9e4ee;border-radius:14px;padding:22px;line-height:1.6;max-height:70vh;overflow:auto;font-size:13px}.provenance{position:sticky;top:24px}.provenance h2{margin-top:0}.provenance article{border-left:3px solid #765ee8;padding:4px 13px;margin:16px 0}.provenance article p{line-height:1.5}.save-status{font-size:12px;color:#16714d;min-height:16px}.graph{display:flex;align-items:center;gap:18px;overflow:auto;padding:80px 20px}.node{min-width:150px;text-align:center;background:white;border:2px solid #7660df;border-radius:13px;padding:25px}.arrow{font-size:28px;color:#7c8795}.links{display:flex;gap:8px;flex-wrap:wrap}.button,button{border:0;background:#182332;color:white;padding:8px 11px;border-radius:8px;font-size:12px;cursor:pointer}.help{font-size:12px;color:#6b7788}@media(max-width:1050px){.context{grid-template-columns:1fr}.provenance{position:static}.grid{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:850px){.shell{grid-template-columns:1fr}.rail{height:auto;position:static;padding:16px 20px}.rail p{display:none}.rail nav{display:flex;overflow:auto;margin-top:12px}.rail nav a{white-space:nowrap}.main{padding:22px 18px}.grid{grid-template-columns:1fr}.board{grid-template-columns:repeat(4,260px)}.top{align-items:flex-start}}`;
function page(title:string,active:string,body:string){const forms=`.action-form{display:flex;gap:10px;align-items:end;flex-wrap:wrap;margin:14px 0}.action-form label{display:grid;gap:5px;font-size:11px;color:#6b7788}.action-form input,.action-form select,.action-form textarea{padding:9px 10px;border:1px solid #ccd4df;border-radius:8px;background:#fff;color:#17202d}.action-form input:focus,.action-form select:focus,.action-form textarea:focus{outline:3px solid #7660df22;border-color:#7660df}.action-form textarea{width:100%;min-height:78px;resize:vertical}.form-error{color:#a71919;font-size:12px}.pictures{display:flex;gap:10px;flex-wrap:wrap}.pictures img{width:150px;height:100px;object-fit:cover;border-radius:9px;border:1px solid #dfe4eb}`;const script=`<script>document.addEventListener('DOMContentLoaded',()=>{const select=document.querySelector('form[data-endpoint="/v1/work-items"] select[name="type"]'),type=document.querySelector('[data-current-type]')?.getAttribute('data-current-type'),next={project:'initiative',initiative:'epic',epic:'story',story:'task'}[type];if(select){if(next)select.innerHTML='<option>'+next+'</option>';else select.closest('form')?.remove()}});const setText=(selector,value)=>{const node=document.querySelector(selector);if(node)node.textContent=value??''};document.addEventListener('submit',async e=>{const f=e.target;if(!(f instanceof HTMLFormElement)||!f.dataset.endpoint)return;e.preventDefault();const error=f.querySelector('.form-error'),status=f.querySelector('.save-status'),csrf=document.cookie.split('; ').find(x=>x.startsWith('aiwork_csrf='))?.split('=')[1];if(error)error.textContent='';if(status)status.textContent='Saving…';if(!csrf){location.href='https://192.168.0.50/admin/sso/aiwork';return}try{const data=new FormData(f),multipart=f.dataset.encoding==='multipart',headers={'x-aiwork-csrf':decodeURIComponent(csrf)};if(!multipart)headers['content-type']='application/json';const response=await fetch(f.dataset.endpoint,{method:f.dataset.method||'POST',headers,body:multipart?data:JSON.stringify(Object.fromEntries(data))});const value=await response.json();if(response.status===401){location.href='https://192.168.0.50/admin/sso/aiwork';return}if(!response.ok)throw new Error(value.error?.message||'Request failed');if(f.dataset.updateWorkItem==='true'){const refreshed=await fetch('/v1/work-items/'+encodeURIComponent(value.id)+'/ai?mode=full').then(r=>{if(!r.ok)throw new Error('Saved, but the updated work item could not be loaded.');return r.json()});const item=refreshed.workItem,details=refreshed.details;f.elements.title.value=item.title;f.elements.description.value=details.description??'';f.elements.aiInstructions.value=item.aiInstructions??'';document.querySelectorAll('input[name="expectedVersion"]').forEach(input=>input.value=String(item.version));setText('[data-work-item-title]',item.title);setText('[data-effective-context]',refreshed.effectiveInstructions);const provenance=document.querySelector('[data-provenance-items]');if(provenance){provenance.replaceChildren(...refreshed.context.provenance.map(entry=>{const article=document.createElement('article'),level=document.createElement('span'),copy=document.createElement('p');level.className='eyebrow';level.textContent=entry.level;copy.textContent=entry.instructions;article.append(level,copy);return article}))}if(status)status.textContent='Saved';return}location.href=(f.dataset.redirect||location.pathname).replace('{id}',value.id)}catch(x){if(status)status.textContent='';if(error)error.textContent=x instanceof Error?x.message:String(x)}})</script>`;return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)} · AIWork</title><style>${css}${forms}</style></head><body><div class="shell"><aside class="rail"><div class="brand">AI<i>Work</i></div><p>Context-driven operations</p><form class="site-search" action="/search"><input name="q" required aria-label="Search all work" placeholder="Search projects and work"><button>Search</button></form><nav><a class="${active==='projects'?'active':''}" href="/projects">Projects</a><a href="https://192.168.0.50/admin/sso/aiwork">Sign in</a><a href="/v1/ai">Agent discovery</a><a href="/docs">API docs</a></nav></aside><main class="main">${body}</main></div>${script}</body></html>`}
function scopedPath(db:Sqlite,id:string){const rows=db.prepare(`WITH RECURSIVE chain(id,parent_id,key,type_id) AS (SELECT id,parent_id,key,type_id FROM work_items WHERE id=? UNION ALL SELECT w.id,w.parent_id,w.key,w.type_id FROM work_items w JOIN chain c ON c.parent_id=w.id) SELECT chain.key,t.key type FROM chain JOIN work_item_types t ON t.id=chain.type_id ORDER BY t.level`).all(id) as Array<{key:string;type:string}>;const order=['project','initiative','epic','story'];if(!rows.length||rows.some((row,index)=>row.type!==order[index]))return `/v1/work-items/${encodeURIComponent(id)}`;let path=`/v1/projects/${encodeURIComponent(rows[0]!.key)}`;for(const row of rows.slice(1))path+=`/${row.type==='story'?'stories':`${row.type}s`}/${encodeURIComponent(row.key)}`;return path;}
export function registerGui(app:FastifyInstance,db:Sqlite,contexts:AiContextService){
  app.get('/',async(_req,reply)=>reply.redirect('/projects'));
  app.get('/projects',async(_req,reply)=>{const projects=db.prepare(`SELECT w.id,w.key,w.title,w.description,(SELECT count(*) FROM work_items x WHERE x.project_id=w.id) item_count FROM work_items w WHERE w.parent_id IS NULL`).all() as Array<Record<string,unknown>>;return reply.type('text/html').send(page('Projects','projects',`<div class="top"><div><p class="eyebrow">Portfolio signal</p><h1>Projects</h1></div><span class="pill">AI context online</span></div><form class="card action-form" data-endpoint="/v1/projects" data-redirect="/projects/{id}/board"><label>Project key<input name="key" required placeholder="WEB"></label><label>Project title<input name="title" required placeholder="Website redesign"></label><button>Create project</button><span class="form-error"></span></form><section class="grid">${projects.map(p=>`<a class="card" href="/projects/${esc(p.id)}/board"><span class="eyebrow">${esc(p.key)}</span><h2>${esc(p.title)}</h2><p class="muted">${esc(p.description)}</p><div class="stat">${esc(p.item_count)}</div><small class="muted">context-linked items</small></a>`).join('')}</section>`))});
  app.get('/search',async(req,reply)=>{const q=String((req.query as {q?:string}).q??'').trim(),like=`%${q}%`;const items=q?db.prepare(`SELECT w.id,w.key,w.title,w.project_id projectId,t.key type,s.key status FROM work_items w JOIN work_item_types t ON t.id=w.type_id JOIN workflow_states s ON s.id=w.state_id WHERE w.key LIKE ? OR w.title LIKE ? ORDER BY w.updated_at DESC LIMIT 100`).all(like,like) as Array<Record<string,unknown>>:[];return reply.type('text/html').send(page('Search','',`<p class="eyebrow">Across every project</p><h1>Search</h1><form class="card action-form" action="/search"><label style="flex:1">Key or title<input name="q" value="${esc(q)}" required autofocus></label><button>Search</button></form>${q?`<p class="muted">${items.length} result${items.length===1?'':'s'} for “${esc(q)}”</p>`:''}<section class="tree">${items.map(i=>`<a href="/work-items/${esc(i.id)}"><span class="type">${esc(i.type)}</span><strong>${esc(i.key)}</strong><span>${esc(i.title)}</span><span class="pill">${esc(i.status)}</span></a>`).join('')||'<p class="muted" style="padding:18px">No matching work items.</p>'}</section>`))});
  app.get('/projects/:id/hierarchy',async(req,reply)=>{const {id}=req.params as {id:string};const items=db.prepare(`WITH RECURSIVE tree(id,parent_id,key,title,type_id,state_id,depth) AS (SELECT id,parent_id,key,title,type_id,state_id,0 FROM work_items WHERE id=? UNION ALL SELECT w.id,w.parent_id,w.key,w.title,w.type_id,w.state_id,t.depth+1 FROM work_items w JOIN tree t ON w.parent_id=t.id) SELECT tree.*,wt.key type,ws.key status FROM tree JOIN work_item_types wt ON wt.id=tree.type_id JOIN workflow_states ws ON ws.id=tree.state_id ORDER BY depth,key`).all(id) as Array<Record<string,unknown>>;return reply.type('text/html').send(page('Context map','hierarchy',`<p class="eyebrow">Inheritance topology</p><h1>Context map</h1><section class="tree">${items.map(i=>`<a href="/work-items/${esc(i.id)}" style="padding-left:${15+Number(i.depth)*24}px"><span class="type">${esc(i.type)}</span><strong>${esc(i.key)}</strong><span>${esc(i.title)}</span><span class="pill">${esc(i.status)}</span></a>`).join('')}</section>`))});
  app.get('/projects/:id/board',async(req,reply)=>{const {id}=req.params as {id:string};const items=db.prepare(`SELECT w.id,w.key,w.title,s.key status,t.key type,a.name actor FROM work_items w JOIN workflow_states s ON s.id=w.state_id JOIN work_item_types t ON t.id=w.type_id LEFT JOIN actors a ON a.id=w.active_actor_id WHERE w.project_id=? AND w.parent_id IS NOT NULL ORDER BY w.created_at`).all(id) as Array<Record<string,unknown>>;const states=['ready','in_progress','test','production'];return reply.type('text/html').send(page('Flow board','board',`<div class="top"><div><p class="eyebrow">Human + agent flow</p><h1>Flow board</h1></div><div class="links"><a class="button" href="/work-items/${esc(id)}">Add work item</a><a class="button" href="/projects/${esc(id)}/hierarchy">Context map</a><a class="button" href="/workflows/wf_default/designer">Edit flow</a></div></div><section class="board">${states.map(state=>`<div class="lane"><h2>${esc(state.replace('_',' '))}<span>${items.filter(i=>i.status===state).length}</span></h2>${items.filter(i=>i.status===state).map(i=>`<a class="ticket" href="/work-items/${esc(i.id)}"><small>${esc(i.key)} · ${esc(i.type)}</small><strong>${esc(i.title)}</strong>${i.actor?`<span class="actor">AI ACTOR · ${esc(i.actor)}</span>`:''}</a>`).join('')}</div>`).join('')}</section>`))});
  app.get('/work-items/:id',async(req,reply)=>{const {id}=req.params as {id:string},context=contexts.get(id,'full',2000) as any;if(!context)return reply.code(404).send('Not found');const ancestors=db.prepare(`WITH RECURSIVE chain(id,parent_id,key,title,type_id,depth) AS (SELECT id,parent_id,key,title,type_id,0 FROM work_items WHERE id=? UNION ALL SELECT w.id,w.parent_id,w.key,w.title,w.type_id,chain.depth+1 FROM work_items w JOIN chain ON chain.parent_id=w.id) SELECT chain.id,chain.key,chain.title,t.key type,chain.depth FROM chain JOIN work_item_types t ON t.id=chain.type_id ORDER BY chain.depth DESC`).all(id) as Array<{id:string;key:string;title:string;type:string;depth:number}>;const parent=ancestors.at(-2),breadcrumbs=`<nav class="breadcrumbs" aria-label="Work item hierarchy">${ancestors.map((item,index)=>`${index?'<span class="separator" aria-hidden="true">›</span>':''}${item.id===id?`<span aria-current="page">${esc(item.key)} · ${esc(item.type)}</span>`:`<a href="/work-items/${esc(item.id)}" title="${esc(item.title)}">${esc(item.key)} · ${esc(item.type)}</a>`}`).join('')}</nav>`;const details=context.details as {description:string|null;comments:Array<{id:string;authorId:string;body:string}>;attachments:Array<{id:string;filename:string;mimeType:string}>},endpoint=scopedPath(db,id),actions=context.availableActions as Array<{toState:string}>,workflow=actions.length?`<h2>Workflow</h2><form class="card action-form" data-endpoint="/v1/work-items/${esc(id)}/transition"><input type="hidden" name="expectedVersion" value="${esc(context.workItem.version)}"><label>Move to<select name="toState">${actions.map(action=>`<option value="${esc(action.toState)}">${esc(action.toState.replace('_',' '))}</option>`).join('')}</select></label><button>Change status</button><span class="form-error"></span></form>`:'';return reply.type('text/html').send(page(String(context.workItem.title),'',`<div class="top"><div data-current-type="${esc(context.workItem.type)}">${breadcrumbs}${parent?`<a class="help" href="/work-items/${esc(parent.id)}">↑ Navigate up to ${esc(parent.key)}</a>`:''}<h1 data-work-item-title>${esc(context.workItem.title)}</h1></div><span class="pill">${esc(context.workItem.status)}</span></div><section class="context"><div>${workflow}<h2>Edit work item</h2><form class="card action-form" data-endpoint="${esc(endpoint)}" data-method="PUT" data-update-work-item="true"><input type="hidden" name="expectedVersion" value="${esc(context.workItem.version)}"><label>Title<input name="title" required value="${esc(context.workItem.title)}"></label><label style="width:100%">Description<textarea name="description">${esc(details.description)}</textarea></label><label style="width:100%">AI instructions<textarea name="aiInstructions">${esc(context.workItem.aiInstructions)}</textarea></label><button>Save changes</button><span class="save-status" role="status" aria-live="polite"></span><span class="form-error" role="alert"></span></form><h2>Comments</h2>${details.comments.map(c=>`<article><strong>${esc(c.authorId)}</strong><p>${esc(c.body)}</p></article>`).join('')||'<p class="muted">No comments yet.</p>'}<form class="action-form" data-endpoint="${esc(endpoint)}/comments"><label style="width:100%">Add comment<textarea name="body" required></textarea></label><button>Comment</button><span class="form-error"></span></form><h2>Pictures</h2><div class="pictures">${details.attachments.filter(a=>a.mimeType.startsWith('image/')).map(a=>`<a href="/v1/attachments/${esc(a.id)}/content" target="_blank"><img src="/v1/attachments/${esc(a.id)}/content" alt="${esc(a.filename)}"></a>`).join('')||'<p class="muted">No pictures yet.</p>'}</div><form class="action-form" data-endpoint="${esc(endpoint)}/pictures" data-encoding="multipart"><label>Upload picture<input type="file" name="file" accept="image/*" required></label><button>Upload</button><span class="form-error"></span></form><h2>Add child</h2><form class="action-form" data-endpoint="/v1/work-items" data-redirect="/work-items/{id}"><input type="hidden" name="parentId" value="${esc(id)}"><label>Type<select name="type"><option>initiative</option><option>epic</option><option>story</option><option>task</option></select></label><label>Title<input name="title" required></label><button>Create</button><span class="form-error"></span></form><h2>Effective AI context</h2><pre class="prompt" data-effective-context>${esc(context.effectiveInstructions)}</pre></div><aside class="card provenance"><h2>Context provenance</h2><div data-provenance-items>${context.context.provenance.map((p:any)=>`<article><span class="eyebrow">${esc(p.level)}</span><p>${esc(p.instructions)}</p></article>`).join('')}</div><div class="links"><a class="button" href="${esc(endpoint)}/ai">Resource AI</a><a class="button" href="/v1/work-items/${esc(id)}/ai?mode=full">Full JSON context</a></div></aside></section>`))});
  app.get('/workflows/:id/designer',async(req,reply)=>{const {id}=req.params as {id:string};const states=db.prepare('SELECT id,name,key FROM workflow_states WHERE workflow_id=? ORDER BY position').all(id) as Array<Record<string,unknown>>;return reply.type('text/html').send(page('Workflow','workflow',`<p class="eyebrow">Actor activation graph</p><h1>Workflow designer</h1><section class="card graph">${states.map((s,index)=>`${index?'<span class="arrow">→</span>':''}<div class="node"><span class="eyebrow">${esc(s.key)}</span><h2>${esc(s.name)}</h2></div>`).join('')}</section>`))});
}
